{
  "name": "mimers-valv",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted MCP memory server with a LAN-only vault tier",
  "main": "index.mjs",
  "scripts": {
    "start": "node index.mjs",
    "test": "node --test",
    "audit": "node scripts/migrate-v2.mjs --dry-run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "mqtt": "^5.10.0",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=22"
  }
}
